<%# locals: (title:) %>
<div class="max-w-md mx-auto my-8">
  <div class="gw-window gw-frame">
    <div class="gw-window-header"><h1 class="gw-title text-base m-0"><%= title %></h1></div>
    <div class="p-5 space-y-4">
      <%= yield %>
    </div>
  </div>
</div>
